<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tremolo || Guitar e-learning</title>
    <link rel="stylesheet" href="/css/animate.css">
    <link rel="stylesheet" href="/css/main.css">
    <link rel="icon" type="image/x-icon" href="images/favicon.ico">
</head>
<body>
    <%- include('header_login',{user_render}); %>
    <aside>

    </aside>
    <main>
        <p>Welcome back <b><%= user_render.name %></b></p>
        <p><%= user_render.id %></p>
        <p><%= userID %></p>
        <form action="/change_pic" method="POST" enctype="multipart/form-data">
            <img src="user_photo/<% if(user_render.user_pic == 1){%><%= user_render._id%>%><%}else{%>default_pic<%}%>.jpg" alt="<%= user_render.name %>" width="40" height="40" class="user_photo">
            <input type="file">
        </form>
    </main>
    <script src="js/main.js"></script>
</body>
</html>