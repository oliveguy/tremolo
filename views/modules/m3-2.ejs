<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tremolo || Guitar e-learning</title>
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/course.css">
    <link rel="stylesheet" href="/css/animate.css">
    <link rel="icon" type="image/x-icon" href="../images/favicon.ico">
</head>
<body>
    <%- include('../header_login',{user_render}); %>
    <div class="body_wrapper">
      <%- include('course_aside',{user_render}); %>
        <main class="course_contents hidden_title">
          <span id="expand_aside"><img src="/images/expand_icon.png" alt="expand_icon" width="25" height="25"></span>
          <div class="content_area">
            <video src="<%=videoURL%>3-2.mp4" width="100%" height="auto" controls id="videoplay"></video>
            <h3>Module. 3 G, C, D Chords</h3>
            <p>In this video, you will learn the G chord, C chord, D chord, then transitioning. </p>
            <ul class="contentList">
              <li>G chord - (pause)</li>
              <li>C chord - (pause)</li>
              <li>D chord - (pause)</li>
              <li>Transitioning</li>
            </ul>
            <form action="/progressCheck" method="POST" id="continueForm">
              <label for="6.25"></label>
              <input type="hidden" value="6.25" name="percentProgress">
              <label for="3"></label>
              <input type="hidden" value="3" name="moduleNum">
              <label for="3"></label>
              <input type="hidden" value="3" name="subModuleNum">
              <label for="<%= user_render.id %>"></label>
              <input type="hidden" name="user_id" value="<%= user_render.id %>">

              <input type="submit" value="Check Completed" id="continueBtn">
            </form>
          </div>
        </main>
    </div>
    <script src="../js/course.js"></script>
    <script>
          vid.addEventListener('timeupdate',function(){
        let num=vid.currentTime;
        let time=Math.round(num*100)/100;
        if(time>=70.85&&time<=71.6){
            vid.pause();
        }
        if(time>=113.9&&time<=114.65){
            vid.pause();
        }
        if(time>=158.4&&time<=159.15){
            vid.pause();
        }
    })
    vid.addEventListener('play',function(){
        let num=vid.currentTime;
        let time=Math.round(num*100)/100;
        if(time>=70.85&&time<=71.6){
                vid.currentTime=71.61;
        }
        if(time>=113.9&&time<=114.65){
            vid.currentTime=114.66;
        }
        if(time>=158.4&&time<=159.15){
            vid.currentTime=159.16;
        }
    })
    </script>
</body>
</html>